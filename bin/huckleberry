#!/usr/bin/env ruby
require 'english'
require_relative '../lib/log_sifter'
require_relative '../lib/usage_output'

if ARGV.first.nil?
  UsageOutput.show_usage
else
  LogSifter.new(ARGV.first).run_script(ARGV[1] ||= "email")
end
